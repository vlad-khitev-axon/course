/* eslint-disable */

// Function declaration
// Classic way for oldfags
function multiply(a, b) {
  return a * b;
}

// Arrow function expression
// Hipster way
const multiply = (a, b) => a * b

function complexAlgorithm(a, b, precision = 0.001) { }

complexAlgorithm(10, 20)
complexAlgorithm(10, 20, 0.000001)
